<html>

<head>
    <link rel="stylesheet" href="{{ asset('assets/css/navbar.css') }}" />
</head>
<body>

<h1>The Genus  {{ name }}</h1>
<div id="menu">
    {% include('navbar.html.twig') %}
</div>
<div id="display_div">
    {% if type == "character" %}
        {{ dump() }}
        {% include('perso.html.twig') %}
    {% elseif type == "newCharacter" %}
        {% include('newperso.html.twig') %}
    {% endif %}
</div>
</body>

</html>